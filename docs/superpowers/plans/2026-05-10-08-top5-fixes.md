# 智能问数系统 v1.0 — 子计划 #8：上线前 Top-5 必修

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复 v1.0 全栈审查报出来的 5 个真正阻塞上线的问题：
1. `sqlai-api` 没进 docker-compose；
2. 多轮对话失忆（history 硬编码空）；
3. EXPLAIN 自修复回路缺位（spec § 4 核心机制）；
4. 前端两处接线漏掉（CSV 链接 + metrics_recommend）；
5. GoldenSet 题库太薄 + `classify` skill 缺位。

**Architecture:** 不引入新 crate / 服务，全部增量修改：
- API 容器化：新增 `crates/sqlai-api/Dockerfile` + 修 `docker-compose.yml`
- pipeline `drive()` 包一层 select-validate-retry 循环；selector 加可选 `feedback` 入参
- `routes/sessions.rs::ask` 在 push user_msg 之前拉历史并喂 pipeline
- frontend `Chat.tsx` 把 `messageId` 传给 `AssistantPanel`；`types.ts` 修 metrics_recommend
- 新增 `sqlai-skills::ml::classify_logreg` + runner 加一条 classify 结果合并路径
- 题库扩到 20+ 题（带不同 schema 假设，方便起步）

**前置假设：**
- v1.0 全部完成（49 commit）。
- 测试时 sidecar 在跑（已缓存 BGE-M3）、ClickHouse 跑、PG 跑、`ch_local` datasource 已注册并 sync-schema 过。

---

## Task 1：sqlai-api Dockerfile + 接入 docker-compose

**Files:**
- Create: `crates/sqlai-api/Dockerfile`
- Create: `crates/sqlai-api/.dockerignore`
- Modify: `docker-compose.yml`

- [ ] **Step 1：Dockerfile（多阶段，rust:bookworm-slim 作 builder + debian:bookworm-slim 作 runtime）**

```dockerfile
# 多阶段：cargo build → debian slim 运行时
FROM m.daocloud.io/docker.io/library/rust:1.83-bookworm AS build

WORKDIR /opt/sqlai

# 国内 crates 镜像加速（与项目本地 .cargo/config.toml 一致）
RUN mkdir -p /usr/local/cargo/registry && \
    cat <<EOF > /usr/local/cargo/config.toml
[source.crates-io]
replace-with = "rsproxy-sparse"

[source.rsproxy-sparse]
registry = "sparse+https://rsproxy.cn/index/"

[net]
git-fetch-with-cli = true
EOF

COPY Cargo.toml Cargo.lock rust-toolchain.toml ./
COPY crates ./crates
COPY migrations ./migrations

RUN cargo build --release --bin sqlai-api && \
    strip target/release/sqlai-api


FROM m.daocloud.io/docker.io/library/debian:bookworm-slim

RUN apt-get update && \
    apt-get install -y --no-install-recommends ca-certificates libssl3 && \
    rm -rf /var/lib/apt/lists/*

WORKDIR /opt/sqlai

COPY --from=build /opt/sqlai/target/release/sqlai-api /usr/local/bin/sqlai-api
COPY --from=build /opt/sqlai/migrations /opt/sqlai/migrations

EXPOSE 8080
ENV RUST_LOG=info,sqlai=debug

CMD ["sqlai-api"]
```

- [ ] **Step 2：.dockerignore**

```
target
.git
node_modules
web
sidecar
docs
```

(放在 `crates/sqlai-api/.dockerignore` 旁，但 `build context: .` 时根目录的 `.dockerignore` 也会被读。这里我们 build context 用 repo 根，下一步在 compose 里指定 `dockerfile`。)

实际：把 `.dockerignore` 放在 **repo 根**：`D:\workspase\rust\sqlai\.dockerignore`，内容同上。删除 `crates/sqlai-api/.dockerignore` 的占位（不需要）。

- [ ] **Step 3：在 docker-compose.yml 加 api 服务**

在 `services:` 下追加：

```yaml
  api:
    build:
      context: .
      dockerfile: crates/sqlai-api/Dockerfile
    container_name: sqlai-api
    ports:
      - "8080:8080"
    depends_on:
      postgres:
        condition: service_healthy
      sidecar:
        condition: service_started
    environment:
      SQLAI_PG_URL: "postgres://sqlai:sqlai@postgres:5432/sqlai"
      SIDECAR_URL: "http://sidecar:8081"
      CLICKHOUSE_URL: "http://host.docker.internal:8123"
      CLICKHOUSE_USER: "admin"
      # CLICKHOUSE_PASSWORD 从主机环境继承，不在 compose 里写
      CLICKHOUSE_DB: "default"
      # DEEPSEEK_API_KEY 从主机环境继承
    extra_hosts:
      - "host.docker.internal:host-gateway"
```

并把现有 `frontend` 服务的 nginx.conf `host.docker.internal:8080` 改为 `api:8080`（因为现在 api 也是容器服务，可以走容器 DNS）：

`web/nginx.conf`：把 `proxy_pass http://host.docker.internal:8080;` 改为 `proxy_pass http://api:8080;`，并在 frontend 服务加 `depends_on: [api]`。

> 实际：保留 `host.docker.internal` 也行，但容器内 `api` 服务名更稳。两种都试，看哪个 work；DNS 优先。

- [ ] **Step 4：build + 起来**

```powershell
docker compose build api 2>&1 | Select-Object -Last 15
$env:DEEPSEEK_API_KEY="sk-..."
$env:CLICKHOUSE_PASSWORD="root23"
docker compose --env-file <(printf "DEEPSEEK_API_KEY=$env:DEEPSEEK_API_KEY`nCLICKHOUSE_PASSWORD=$env:CLICKHOUSE_PASSWORD") up -d api
```

PowerShell 的 `--env-file` 输入有点 hack，改用更直接的方式：把 key 临时写入 `.env`，跑完删除：

```powershell
"DEEPSEEK_API_KEY=$env:DEEPSEEK_API_KEY" | Out-File -FilePath .env -Encoding utf8
"CLICKHOUSE_PASSWORD=$env:CLICKHOUSE_PASSWORD" | Add-Content -Path .env
docker compose up -d api
Remove-Item .env
```

> ⚠️ 把 `.env` 放进 `.gitignore`（已经在；之前的 .gitignore 已含 `.env`）。

- [ ] **Step 5：验证**

```powershell
Start-Sleep -Seconds 5
Invoke-WebRequest -Uri "http://127.0.0.1:8080/healthz" -UseBasicParsing | Select-Object -ExpandProperty Content
```

预期：`{"ok":true}`。

- [ ] **Step 6：commit**

```
git add .dockerignore crates/sqlai-api/Dockerfile docker-compose.yml web/nginx.conf
git commit -m "chore(devenv): containerize sqlai-api and wire into docker-compose"
```

---

## Task 2：多轮对话历史（fetch → pass to pipeline）

**Files:**
- Modify: `crates/sqlai-api/src/routes/sessions.rs`

- [ ] **Step 1：在 ask handler 里把历史拉出来塞进 pipeline**

把 `ask()` 中"先 insert user_msg，然后 build req"的顺序改成"先拉历史，再 insert user_msg，再 build req"：

找到这段：
```rust
let user_msg = sqlai_store::session::append_message(
    &s.pool,
    NewMessage {
        session_id: sess.id,
        role: "user".into(),
        content: serde_json::json!({ "text": body.question }),
        ...
    },
)
.await?;

let req = AskRequest {
    session_id: sess.id,
    datasource_id,
    question: body.question.clone(),
    history: vec![],
};
```

改为：

```rust
// 1. 先拉历史（不含本次问题）
let history_msgs = sqlai_store::session::list_messages(&s.pool, sess.id)
    .await
    .unwrap_or_default();
let history: Vec<sqlai_llm::ChatMessage> = history_msgs
    .iter()
    .filter_map(|m| {
        let text = match m.role.as_str() {
            "user" => m
                .content
                .get("text")
                .and_then(|v| v.as_str())
                .map(String::from),
            "assistant" => m
                .content
                .get("summary")
                .and_then(|v| v.as_str())
                .map(String::from),
            _ => None,
        };
        text.map(|content| sqlai_llm::ChatMessage {
            role: m.role.clone(),
            content,
        })
    })
    .collect();

// 2. 再 insert user_msg
let user_msg = sqlai_store::session::append_message(
    &s.pool,
    NewMessage {
        session_id: sess.id,
        role: "user".into(),
        content: serde_json::json!({ "text": body.question }),
        plan: None,
        chart_spec: None,
        rows_returned: None,
        latency_ms: None,
        parent_id: None,
    },
)
.await?;

// 3. 拼 ask 请求
let req = AskRequest {
    session_id: sess.id,
    datasource_id,
    question: body.question.clone(),
    history,
};
```

- [ ] **Step 2：build + commit**

```
cargo build -p sqlai-api 2>&1 | tail -5
git add crates/sqlai-api
git commit -m "fix(api): fetch session history and pass to pipeline (multi-turn)"
```

> 端到端验证留到 Task 5 一起做。

---

## Task 3：EXPLAIN 自修复回路（spec § 4 核心机制）

**Files:**
- Modify: `crates/sqlai-pipeline/src/runner.rs`（拆出 `validate_plan` 与 `run_validated`）
- Modify: `crates/sqlai-pipeline/src/selector.rs`（加 `feedback` 可选入参）
- Modify: `crates/sqlai-pipeline/src/lib.rs`（在 `drive` 中加 retry 循环）

- [ ] **Step 1：runner.rs 暴露 `validate_plan_only`**

在现有 `validate_and_run` 上方添加：

```rust
/// 只做本地 SELECT-only 校验 + 远端 EXPLAIN，不执行。
/// 让 pipeline 在 select 阶段就能感知 SQL 是否合法，做 EXPLAIN 自修复。
pub async fn validate_plan_only(
    executor: &Arc<dyn Executor>,
    plan: &AnalysisPlan,
) -> Result<(), RunnerError> {
    for (idx, step) in plan.steps.iter().enumerate() {
        if let AnalysisStep::Sql(s) = step {
            let validated = validate(&s.sql).map_err(|e: ValidationError| RunnerError::Validate {
                step_idx: idx,
                err: e.to_string(),
            })?;
            executor
                .explain(&validated)
                .await
                .map_err(|e: ExecError| RunnerError::Explain {
                    step_idx: idx,
                    err: e.to_string(),
                })?;
        }
    }
    Ok(())
}
```

- [ ] **Step 2：selector.rs 加 feedback 入参**

替换 `select_and_plan` 的签名，把 feedback 作为第 5 个可选参数：

```rust
pub async fn select_and_plan(
    llm: &Arc<dyn LlmProvider>,
    skills: &SkillRegistry,
    question: &str,
    ctx: &RetrievalContext,
    feedback: Option<&str>, // 上一次 EXPLAIN 失败的错误，作为反馈
) -> Result<(String, serde_json::Value, AnalysisPlan), SelectError> {
    // ... 原 tools / schema_md 不变 ...

    let mut messages = vec![
        ChatMessage { role: "system".into(), content: SYSTEM_PROMPT.into() },
        ChatMessage { role: "system".into(), content: format!("可用 schema 与知识：\n{schema_md}") },
    ];
    if let Some(err) = feedback {
        messages.push(ChatMessage {
            role: "system".into(),
            content: format!(
                "上一次尝试失败，错误信息：\n{}\n请基于该错误调整你的 tool_call 参数（例如改用提供的 schema 中真实存在的列名）。",
                err
            ),
        });
    }
    messages.push(ChatMessage { role: "user".into(), content: question.to_string() });

    // 后续 ChatRequest / 解析逻辑保持不变
    // ...
}
```

并把现有 `let messages = vec![...]` 删除，用上面新的 mut 版本。

- [ ] **Step 3：lib.rs `drive()` 包 retry 循环**

把现有这段：

```rust
let (skill_name, args, plan) = selector::select_and_plan(&self.llm, &self.skills, &req.question, &ctx)
    .await
    .map_err(|e| StageErr::new("select", "skill", e.to_string()))?;
let _ = tx.send(PipelineEvent::SkillCall { ... }).await;

let runs = runner::validate_and_run(&self.executor, self.ml_client.as_ref(), &plan)
    .await
    .map_err(|e| {
        let stage = match &e {
            runner::RunnerError::Validate { .. } | runner::RunnerError::Explain { .. } => "validate",
            runner::RunnerError::Execute { .. } => "execute",
            _ => "execute",
        };
        StageErr::new(stage, "exec", e.to_string())
    })?;
let _ = tx.send(PipelineEvent::Validate { passed: true, retries: 0, error: None }).await;
```

替换为：

```rust
const MAX_RETRIES: u32 = 2;
let mut last_err: Option<String> = None;
let mut chosen: Option<(String, serde_json::Value, sqlai_skills::AnalysisPlan)> = None;
let mut retries: u32 = 0;
for attempt in 0..=MAX_RETRIES {
    let (skill_name, args, plan) = selector::select_and_plan(
        &self.llm, &self.skills, &req.question, &ctx, last_err.as_deref(),
    )
    .await
    .map_err(|e| StageErr::new("select", "skill", e.to_string()))?;
    match runner::validate_plan_only(&self.executor, &plan).await {
        Ok(()) => {
            chosen = Some((skill_name, args, plan));
            break;
        }
        Err(e) => {
            last_err = Some(e.to_string());
            retries = attempt + 1;
            let _ = tx
                .send(PipelineEvent::Validate {
                    passed: false,
                    retries,
                    error: Some(e.to_string()),
                })
                .await;
            if attempt == MAX_RETRIES {
                return Err(StageErr::new("validate", "exec", e.to_string()));
            }
        }
    }
}
let (skill_name, args, plan) = chosen.expect("loop exited without choice or error");

let _ = tx
    .send(PipelineEvent::SkillCall {
        skill: skill_name,
        args,
        plan: PlanSnapshot {
            steps: plan
                .steps
                .iter()
                .map(|s| match s {
                    sqlai_skills::AnalysisStep::Sql(x) => SqlStepSnapshot::from(x),
                    sqlai_skills::AnalysisStep::Compute(c) => SqlStepSnapshot {
                        label: c.label.clone(),
                        sql: format!("[compute:{:?}]", c.function),
                    },
                    sqlai_skills::AnalysisStep::Ml(m) => SqlStepSnapshot {
                        label: m.label.clone(),
                        sql: format!("[ml:{}]", m.task),
                    },
                })
                .collect(),
            explanation: plan.explanation.clone(),
        },
    })
    .await;

let runs = runner::validate_and_run(&self.executor, self.ml_client.as_ref(), &plan)
    .await
    .map_err(|e| {
        let stage = match &e {
            runner::RunnerError::Validate { .. } | runner::RunnerError::Explain { .. } => "validate",
            runner::RunnerError::Execute { .. } => "execute",
            _ => "execute",
        };
        StageErr::new(stage, "exec", e.to_string())
    })?;
let _ = tx
    .send(PipelineEvent::Validate {
        passed: true,
        retries,
        error: None,
    })
    .await;
```

注意：`validate_and_run` 还会再 validate+explain+run 一次（重复 validate 不影响正确性，只是多一次 EXPLAIN 调 CH，可接受）。

- [ ] **Step 4：build + 单元测试**

```
cargo build -p sqlai-pipeline 2>&1 | tail -5
cargo test --workspace 2>&1 | tail -10
```

预期：编译通过，原单元测试不破。

- [ ] **Step 5：commit**

```
git add crates/sqlai-pipeline
git commit -m "feat(pipeline): EXPLAIN self-repair loop (max 2 retries) feeding error back to selector"
```

---

## Task 4：前端两处接线漏掉（CSV link + metrics_recommend）

**Files:**
- Modify: `web/src/api/types.ts`
- Modify: `web/src/pages/Chat.tsx`
- Modify: `web/src/components/chat/AssistantPanel.tsx`

- [ ] **Step 1：types.ts 修 metrics_recommend 形状**

后端发的事件 payload 是顶层数组（`PipelineEvent::MetricsRecommend(Vec<MetricRecommendation>)`），不是 `{ data: [...] }`。改 types.ts：

```ts
export interface MetricRecommendation {
  name: string
  reason: string
}

export interface MetricsRecommendEvent {
  event: 'metrics_recommend'
  // 后端 SSE 的 data 整体是数组；前端 sse.ts 把整个 JSON 展开到事件对象里，
  // 数组会落在哪个字段取决于 serde 序列化。后端用 #[serde(tag="event")] +
  // 元组变体直接序列化数组，所以前端拿到的对象形如 { event: "metrics_recommend", "0": {...}, "1": {...} } 或
  // 整个事件就是一个数组（取决于 serde 配置）。统一处理：用 raw 字段。
  raw: MetricRecommendation[]
}
```

> **实务：** SSE 事件解析的稳妥做法是看后端真实输出。先打开 `cargo test -p sqlai-api --test api_e2e -- --ignored --nocapture` 输出里 `metrics_recommend` 的 `data:` 行，确认形状。**目前 `metric_recommendations()` 永远返回空数组**（v1.0 占位），所以前端这件事的 cosmetic 修复优先级低。

替代方案：把 `MetricsRecommendEvent` 改为 `{ event: 'metrics_recommend' }`（不带 payload），并在 sse.ts 里特殊处理把 array 装回去。但因为后端目前永远返 `[]`，留 placeholder 也行。

**最小可接受改动：** 把 `MetricsRecommendEvent` 简化为 `{ event: 'metrics_recommend' }`（暂不含 payload），并在 PipelineEvent union 里替换。这避免类型说谎。等子计划 #9 把 `metric_recommendations()` 真正实现时再补类型。

替换 types.ts 中：
```ts
| { event: 'metrics_recommend'; data?: any[] }
```
为：
```ts
| { event: 'metrics_recommend' }
```

- [ ] **Step 2：Chat.tsx 把 messageId 传给 AssistantPanel**

目前 `Chat.tsx` 的 `Turn` 类型不存 messageId（assistant 消息是后端 SSE 流结束后才持久化的，前端无从得知）。**真正修法：** 让 `done` 事件回传 message_id。

修后端 `crates/sqlai-pipeline/src/event.rs`：
```rust
PipelineEvent::Done { latency_ms } -> PipelineEvent::Done { latency_ms, message_id: Option<Uuid> }
```

修 `crates/sqlai-api/src/routes/sessions.rs::ask`：spawned task 在 `append_message` 之后，往 sse_tx 多发一个事件 `event: assistant_message_id` 携带 id。但事件流已经关了（rx 用完）—— 这条路走不通。

**简单替代：** 在 SSE 流结束后，前端通过 `GET /api/sessions/:id/messages` 拉一次最新消息，找到 role=assistant 的最后一条，拿其 id。

修 Chat.tsx 的 `ask()` 回调，在 `event === 'done'` 时调用 `listMessages` 并把最后一条 assistant 的 id 写入 turn：

```tsx
import { listMessages } from '../api/client'

// ...

case 'done': {
    cur.done = true
    // 拉最新消息列表，绑 assistant message id 到本 turn
    listMessages(sessionId)
        .then((msgs) => {
            const asst = [...msgs].reverse().find((m) => m.role === 'assistant')
            if (asst) {
                setTurns((curr2) => {
                    const next2 = [...curr2]
                    if (next2[idx]) {
                        next2[idx] = { ...next2[idx], assistantMessageId: asst.id }
                    }
                    return next2
                })
            }
        })
        .catch(() => {})
    break
}
```

并扩展 Turn 类型加 `assistantMessageId?: string`。

最后在 JSX 里把它传给 AssistantPanel：

```tsx
<AssistantPanel
  messageId={t.assistantMessageId}
  skillCall={t.skillCall}
  rows={t.rows}
  chart={t.chart}
  summary={t.summary}
/>
```

- [ ] **Step 3：build**

```
cd D:\workspase\rust\sqlai\web
npm run build 2>&1 | tail -10
```

- [ ] **Step 4：commit**

```
cd D:\workspase\rust\sqlai
git add web/src
git commit -m "fix(web): wire CSV export messageId via post-stream message fetch; tighten metrics_recommend type"
```

---

## Task 5：classify_logreg skill + GoldenSet 扩到 20 题

**Files:**
- Modify: `crates/sqlai-skills/src/ml/cluster_kmeans.rs`（保留）
- Create: `crates/sqlai-skills/src/ml/classify_logreg.rs`
- Modify: `crates/sqlai-skills/src/ml/mod.rs`
- Modify: `crates/sqlai-skills/src/lib.rs`（注册）
- Modify: `crates/sqlai-pipeline/src/runner.rs`（merge_ml_into_rows 兼容 classify 结果）
- Modify: `docs/superpowers/specs/golden-set-example.json`

- [ ] **Step 1：classify_logreg skill**

`crates/sqlai-skills/src/ml/classify_logreg.rs`：

```rust
//! classify_logreg：从一张表抽数值特征 + 整数标签，调 sidecar 跑逻辑回归。

use serde::Deserialize;
use sqlai_core::RetrievalContext;

use crate::error::SkillError;
use crate::plan::{AnalysisPlan, AnalysisStep, ChartHint, ChartKind, MlStep, SqlStep};
use crate::render::quote_ident;
use crate::{AnalysisSkill, SkillSchema};

pub struct ClassifyLogreg;

#[derive(Debug, Deserialize)]
struct Args {
    db: String,
    table: String,
    feature_columns: Vec<String>,
    label_column: String,
    #[serde(default)]
    where_clause: Option<String>,
    #[serde(default = "default_sample_limit")]
    sample_limit: u32,
    #[serde(default = "default_test_size")]
    test_size: f32,
}
fn default_sample_limit() -> u32 { 5000 }
fn default_test_size() -> f32 { 0.2 }

impl AnalysisSkill for ClassifyLogreg {
    fn name(&self) -> &'static str { "classify_logreg" }
    fn description(&self) -> &'static str {
        "对一张表的一组数值特征列 + 一个整数标签列做逻辑回归分类。\
         返回训练集大小、测试集大小与 accuracy。"
    }
    fn schema(&self) -> SkillSchema {
        SkillSchema {
            name: self.name().to_string(),
            description: self.description().to_string(),
            parameters: serde_json::json!({
                "type": "object",
                "properties": {
                    "db":              {"type":"string"},
                    "table":           {"type":"string"},
                    "feature_columns": {"type":"array","items":{"type":"string"},"minItems":1,"maxItems":12},
                    "label_column":    {"type":"string"},
                    "test_size":       {"type":"number","default":0.2,"minimum":0.05,"maximum":0.5},
                    "sample_limit":    {"type":"integer","default":5000,"minimum":50,"maximum":100000},
                    "where_clause":    {"type":"string"}
                },
                "required": ["db","table","feature_columns","label_column"]
            }),
        }
    }
    fn plan(&self, args: &serde_json::Value, _ctx: &RetrievalContext) -> Result<AnalysisPlan, SkillError> {
        let a: Args = serde_json::from_value(args.clone())
            .map_err(|e| SkillError::InvalidArg("classify_logreg", e.to_string()))?;
        if a.feature_columns.is_empty() {
            return Err(SkillError::InvalidArg("feature_columns", "need >= 1".into()));
        }

        // sklearn _logreg expects last column as label
        let mut all = a.feature_columns.clone();
        all.push(a.label_column.clone());
        let table = format!("{}.{}", quote_ident(&a.db), quote_ident(&a.table));
        let select = all.iter().map(|c| quote_ident(c)).collect::<Vec<_>>().join(", ");
        let where_sql = match &a.where_clause {
            Some(w) if !w.trim().is_empty() => format!(" WHERE ({w})"),
            _ => String::new(),
        };
        let sql = format!("SELECT {select} FROM {table}{where_sql} LIMIT {}", a.sample_limit);

        Ok(AnalysisPlan {
            steps: vec![
                AnalysisStep::Sql(SqlStep {
                    label: format!("{}.{} 抽特征+标签", a.db, a.table),
                    sql,
                }),
                AnalysisStep::Ml(MlStep {
                    label: "logistic regression".into(),
                    task: "classify_logreg".into(),
                    source_step: 0,
                    feature_columns: all, // 含 label
                    params: serde_json::json!({"test_size": a.test_size, "random_state": 42}),
                }),
            ],
            chart_hint: Some(ChartHint { kind: ChartKind::None, x: None, y: None }),
            explanation: format!(
                "用 {} 个特征预测 {}，测试集占比 {}",
                a.feature_columns.len(), a.label_column, a.test_size
            ),
        })
    }
}

#[cfg(test)]
mod tests {
    use super::*;
    use sqlai_core::RetrievalContext;

    fn ctx() -> RetrievalContext {
        RetrievalContext { tables: vec![], columns: vec![], business_terms: vec![], few_shots: vec![] }
    }

    #[test]
    fn plan_includes_label_as_last_feature_column() {
        let p = ClassifyLogreg.plan(&serde_json::json!({
            "db":"d","table":"t","feature_columns":["x","y"],"label_column":"is_paid"
        }), &ctx()).unwrap();
        assert_eq!(p.steps.len(), 2);
        if let AnalysisStep::Ml(m) = &p.steps[1] {
            assert_eq!(m.feature_columns, vec!["x".to_string(), "y".to_string(), "is_paid".to_string()]);
            assert_eq!(m.task, "classify_logreg");
        } else {
            panic!("expected Ml step");
        }
    }
}
```

- [ ] **Step 2：mod.rs + lib.rs 注册**

`crates/sqlai-skills/src/ml/mod.rs`：
```rust
pub mod classify_logreg;
pub mod cluster_kmeans;
```

`lib.rs::with_defaults` 末尾加：
```rust
r.register(Arc::new(ml::classify_logreg::ClassifyLogreg));
```

- [ ] **Step 3：runner.rs `merge_ml_into_rows` 兼容 classify**

把现有 `merge_ml_into_rows` 替换为：

```rust
fn merge_ml_into_rows(
    src: &ExecutionResult,
    ml_resp: &serde_json::Value,
    feature_cols: &[String],
) -> Result<ExecutionResult, String> {
    let task = ml_resp.get("task").and_then(|v| v.as_str()).unwrap_or("");
    let result = ml_resp.get("result").ok_or("ml resp missing 'result'")?;

    if task == "classify_logreg" {
        // 单行报表：accuracy / n_train / n_test
        let acc = result.get("accuracy").and_then(|v| v.as_f64()).unwrap_or(0.0);
        let n_train = result.get("n_train").and_then(|v| v.as_i64()).unwrap_or(0);
        let n_test = result.get("n_test").and_then(|v| v.as_i64()).unwrap_or(0);
        let row = serde_json::json!({
            "accuracy": acc,
            "n_train": n_train,
            "n_test": n_test,
        });
        return Ok(ExecutionResult {
            columns: vec!["accuracy".into(), "n_train".into(), "n_test".into()],
            rows: vec![row],
            truncated: false,
        });
    }

    // kmeans 等带 labels 的：把 cluster 列追加到原行
    let labels: Vec<i64> = result
        .get("labels")
        .and_then(|v| v.as_array())
        .map(|arr| arr.iter().filter_map(|v| v.as_i64()).collect())
        .unwrap_or_default();
    if labels.len() != src.rows.len() {
        return Err(format!(
            "ml labels.len()={} doesn't match rows.len()={}",
            labels.len(), src.rows.len()
        ));
    }
    let mut new_cols = feature_cols.to_vec();
    new_cols.push("cluster".into());
    let new_rows: Vec<serde_json::Value> = src.rows.iter().enumerate().map(|(i, r)| {
        let mut o = serde_json::Map::new();
        for c in feature_cols {
            o.insert(c.clone(), r.get(c).cloned().unwrap_or(serde_json::Value::Null));
        }
        o.insert("cluster".into(), serde_json::Value::from(labels[i]));
        serde_json::Value::Object(o)
    }).collect();
    Ok(ExecutionResult {
        columns: new_cols,
        rows: new_rows,
        truncated: false,
    })
}
```

注意：`_task` 列已被移除（审查报告认为它不在 spec 内）。

- [ ] **Step 4：题库扩展**

替换 `docs/superpowers/specs/golden-set-example.json`：

```json
[
  {"id":"D001","question":"看一下 default.orders 按天的订单金额趋势","datasource":"ch_local","expected_skill":"metric_overview","expected_columns":["bucket","value"],"expected_min_rows":1},
  {"id":"D002","question":"近 30 天 default.orders 按周的销量趋势","datasource":"ch_local","expected_skill":"metric_overview","expected_columns":["bucket","value"],"expected_min_rows":1},
  {"id":"D003","question":"default.orders 销售额最高的前 5 个商品","datasource":"ch_local","expected_skill":"topn","expected_columns":["dimension","value"],"expected_min_rows":1},
  {"id":"D004","question":"default.orders 订单数最多的前 10 名用户","datasource":"ch_local","expected_skill":"topn","expected_columns":["dimension","value"],"expected_min_rows":1},
  {"id":"D005","question":"对比 2024-01 与 2024-02 月度 default.orders 总金额","datasource":"ch_local","expected_skill":"compare_period","expected_columns":["current","baseline"],"expected_min_rows":1},
  {"id":"D006","question":"对比上周与本周 default.orders 销售额，按渠道","datasource":"ch_local","expected_skill":"compare_period","expected_columns":["dimension","current","baseline","delta"],"expected_min_rows":0},
  {"id":"D007","question":"default.orders 各商品销售额占比","datasource":"ch_local","expected_skill":"share_breakdown","expected_columns":["dimension","value","share"],"expected_min_rows":1},
  {"id":"D008","question":"default.orders 不同商品按天的销售额走势","datasource":"ch_local","expected_skill":"trend_segment","expected_columns":["bucket","segment","value"],"expected_min_rows":1},
  {"id":"G001","question":"default.orders 销售下滑，按商品+用户拆解差异","datasource":"ch_local","expected_skill":"drill_down","expected_columns":["delta"],"expected_min_rows":0},
  {"id":"G002","question":"default.orders 各列之间的相关性","datasource":"ch_local","expected_skill":"correlation_matrix","expected_columns":["col1","col2","corr"],"expected_min_rows":1},
  {"id":"G003","question":"default.orders 金额分布在 1 月与 2 月之间的差异","datasource":"ch_local","expected_skill":"distribution_shift","expected_columns":["period","lo","hi","c"],"expected_min_rows":0},
  {"id":"F001","question":"未来 7 天 default.orders 销售额预估","datasource":"ch_local","expected_skill":"forecast_simple","expected_columns":["bucket","value","kind"],"expected_min_rows":1},
  {"id":"F002","question":"按周做 default.orders 趋势的 4 周外推","datasource":"ch_local","expected_skill":"forecast_simple","expected_columns":["bucket","value","kind"],"expected_min_rows":1},
  {"id":"M001","question":"对 default.orders 的 amount + quantity 做 K-means k=3","datasource":"ch_local","expected_skill":"cluster_kmeans","expected_columns":["cluster"],"expected_min_rows":0},
  {"id":"M002","question":"用 default.orders.amount,quantity 预测 is_paid","datasource":"ch_local","expected_skill":"classify_logreg","expected_columns":["accuracy"],"expected_min_rows":1},
  {"id":"R001","question":"default.orders 当天最贵的 3 笔订单","datasource":"ch_local","expected_skill":"topn","expected_columns":["dimension","value"],"expected_min_rows":1},
  {"id":"R002","question":"default.orders 月成交金额平均","datasource":"ch_local","expected_skill":"metric_overview","expected_columns":["bucket","value"],"expected_min_rows":1},
  {"id":"R003","question":"default.orders 周环比销售额增长率最高的 5 个商品","datasource":"ch_local","expected_skill":"compare_period","expected_columns":["dimension","delta"],"expected_min_rows":0},
  {"id":"R004","question":"default.products 各品类商品数量占比","datasource":"ch_local","expected_skill":"share_breakdown","expected_columns":["dimension","value","share"],"expected_min_rows":0},
  {"id":"R005","question":"default.orders 按用户分群（K=4）","datasource":"ch_local","expected_skill":"cluster_kmeans","expected_columns":["cluster"],"expected_min_rows":0}
]
```

(共 20 题；许多题对应的 schema 在用户的 demo CH 里可能不存在，会触发 EXPLAIN 失败 → 自修复回路 → 看是否能命中。这就是为什么自修复重要：实战题库不可能 schema 完美匹配。)

- [ ] **Step 5：build + 跑一次 eval**

```
cargo build --workspace
cargo test -p sqlai-skills 2>&1 | tail -5
```

预期：sqlai-skills 23 passed（22 + 1 classify）。

```powershell
$env:SQLAI_PG_URL="postgres://sqlai:sqlai@127.0.0.1:5432/sqlai"
$env:DEEPSEEK_API_KEY="sk-..."
$env:CLICKHOUSE_PASSWORD="root23"
cargo run -p sqlai-cli -- eval --goldenset docs/superpowers/specs/golden-set-example.json --report eval-report-20.json 2>&1 | Select-Object -Last 30
```

预期：报表打印 20 题的命中情况；目标是 skill_acc ≥ 70%。

- [ ] **Step 6：commit**

```
git add crates/sqlai-skills crates/sqlai-pipeline docs/superpowers/specs/golden-set-example.json
git commit -m "feat(skills,pipeline): add classify_logreg skill; expand GoldenSet to 20 items; runner handles classify result"
```

---

## 验收清单

- [ ] `docker compose up -d api` 后 `curl http://localhost:8080/healthz` 返回 200
- [ ] 多轮：`POST /api/sessions/:id/ask` 两次（"GMV 走势" → "再看看上周"），第二次的 LLM 输入里能看到第一次的 user/assistant 历史
- [ ] EXPLAIN 自修复：手动构造一个会失败的题（schema 缺列），观察 SSE 流出现 `validate{passed:false, retries:1}` 然后再次出 `skill_call`，最终成功或两轮后报错
- [ ] 前端：发问题 → done 后 CSV 链接出现且可点
- [ ] `cargo run -p sqlai-cli -- eval` 跑通 20 题，报表 skill_acc ≥ 70%
- [ ] `cargo test --workspace` 全绿
- [ ] `git log` 至少 5 条本子计划 commit

---

## 仍未做的事（v1.x 候选）

- API 鉴权（Bearer / OAuth）
- introspection SQL 改用真正参数化
- 审计日志
- LLM 速率限制 / 退避重试
- 前端 Playwright E2E
- ECharts 主 chunk 大小优化
